{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/rorph/OpenCEPGEO/schemas/cep-geo.schema.json",
  "title": "OpenCEPGeo lookup result",
  "type": "object",
  "required": ["cep", "prefix", "geo"],
  "properties": {
    "cep": {"type": "string", "pattern": "^[0-9]{8}$"},
    "prefix": {"type": "string", "pattern": "^[0-9]{5}$"},
    "ibge": {"type": ["string", "null"], "pattern": "^[0-9]{7}$"},
    "uf": {"type": ["string", "null"]},
    "city": {"type": ["string", "null"]},
    "geo": {
      "oneOf": [
        {"type": "null"},
        {
          "type": "object",
          "required": ["type", "coordinates", "precision", "sample_size", "radius_km", "source"],
          "properties": {
            "type": {"const": "Point"},
            "coordinates": {
              "type": "array",
              "prefixItems": [
                {"type": "number", "minimum": -180, "maximum": 180},
                {"type": "number", "minimum": -90, "maximum": 90}
              ],
              "minItems": 2,
              "maxItems": 2
            },
            "precision": {
              "enum": ["observed_cep", "observed_cep_prefix", "municipality"]
            },
            "sample_size": {"type": "integer", "minimum": 1},
            "radius_km": {"type": ["number", "null"], "minimum": 0},
            "source": {"type": "array", "items": {"type": "string"}}
          },
          "additionalProperties": false
        }
      ]
    }
  }
}

